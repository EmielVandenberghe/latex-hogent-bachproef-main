# Voeg deze sectie toe aan je bestaande prometheus.yml
# onder het scrape_configs: blok.

  - job_name: 'srt_exporter'
    scrape_interval: 15s
    scrape_timeout:  10s
    static_configs:
      - targets:
          - '10.1.1.100:9117'
        labels:
          instance: 'almalinux-vm'
          env:      'poc'
