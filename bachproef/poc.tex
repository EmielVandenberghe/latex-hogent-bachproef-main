%%=============================================================================
%% Proof-of-Concept
%%=============================================================================

\chapter{Proof-of-Concept}
\label{ch:poc}

Dit hoofdstuk beschrijft de concrete uitwerking van het proof-of-concept (PoC) dat voortvloeit uit de methodologie in Hoofdstuk~\ref{ch:methodologie}. Het doel is aantonen dat een gecentraliseerde observability-oplossing op basis van de PLG-stack in staat is om de volledige audiovisuele keten van Mediaventures end-to-end te bewaken: van de onderliggende WAN-laag (Peplink en PepVPN), over de netwerkkwaliteit (ICMP, jitter, packet loss), tot de applicatielaag waar SRT-streams lopen. Achtereenvolgens komen de repositorystructuur, de virtuele testomgeving, de observability-stack, de verzamelde metrics, het Grafana-dashboard, het alerting-mechanisme en een reeks gevalideerde testscenario's aan bod. Het hoofdstuk sluit af met een zelf-validatie van de stack, een overzicht van het resourceverbruik en een expliciete scopeafbakening.

\section{GitHub-repository}
\label{sec:poc-github}

De volledige broncode van het PoC is publiek beschikbaar op GitHub:

\begin{center}
\href{https://github.com/EmielVandenberghe/bachp-mediaventures}{\texttt{github.com/EmielVandenberghe/bachp-mediaventures}}
\end{center}

Het repository bevat alle configuratiebestanden, Python-exporters, Vagrant- en Docker Compose-definities, Grafana-dashboards, alert rules en aanvullende documentatie. De lezer kan het PoC lokaal reproduceren door de stappen in \texttt{poc/omgeving\_opzetten.md} te volgen.

\section{Structuur van de repository}
\label{sec:poc-repo-structuur}

Tabel~\ref{tab:repo-structuur} geeft een overzicht van de mappenstructuur van de belangrijkste componenten. De repository is georganiseerd rond één centrale map \texttt{poc/stack/} waarin alle runtime-componenten samenkomen. De bijhorende documentatie staat als Markdown-bestanden naast de code, zodat elke beslissing herleidbaar is vanuit hetzelfde versiebeheer.

\begin{table}[htbp]
  \centering
  \caption[Structuur van de PoC-repository]{Overzicht van de mappenstructuur van de GitHub-repository.}
  \label{tab:repo-structuur}
  \begin{tabular}{p{0.42\linewidth} p{0.52\linewidth}}
    \hline
    \textbf{Map / bestand} & \textbf{Beschrijving} \\
    \hline
    \texttt{poc/stack/Vagrantfile}                 & Provisioning van de AlmaLinux 9 observability-VM \\
    \texttt{poc/stack/docker-compose.yml}          & Definitie van 12 containers (Prometheus, Grafana, Loki, exporters, SRT, NDI) \\
    \texttt{poc/stack/Dockerfile}                  & Python-image voor de \texttt{incontrol2-exporter} (pysnmp 4.4.12 gepind) \\
    \texttt{poc/stack/Dockerfile.ping}             & Image met \texttt{iputils-ping} voor de jitter-exporter \\
    \texttt{poc/stack/Dockerfile.srt}              & Image met \texttt{srt-live-transmit} voor de SRT-exporter \\
    \texttt{poc/stack/incontrol2\_exporter.py}     & Custom exporter: InControl2 API + SNMP + lokale Peplink REST API \\
    \texttt{poc/stack/ping\_exporter.py}           & Nauwkeurige jitter-meting via \texttt{ping -c 20} (mdev) \\
    \texttt{poc/stack/srt\_exporter.py}            & Parser van \texttt{srt-live-transmit} CSV-stats naar Prometheus \\
    \texttt{poc/stack/prometheus.yml}              & Scrape-configuratie voor alle exporters en blackbox \\
    \texttt{poc/stack/blackbox.yml, loki.yml, promtail.yml} & ICMP-probes, log-opslag en log-collectie \\
    \texttt{poc/stack/provisioning/dashboards/peplink.json} & JSON-definitie van het Grafana-dashboard (11 secties) \\
    \texttt{poc/stack/provisioning/alerting/alerts.yml}      & Provisioned alert rules (10 regels) \\
    \texttt{poc/stack/provisioning/datasources/}             & Datasource-configuratie voor Prometheus en Loki \\
    \texttt{poc/*.md}                              & Documentatie per subsysteem (dashboard, alerting, scenario's, blind-spot) \\
    \hline
  \end{tabular}
\end{table}

\section{Virtuele testomgeving}
\label{sec:poc-omgeving}

Omdat testen op de productie-infrastructuur van Mediaventures niet toegelaten is tijdens live-surgery projecten, werd een volledig virtuele testomgeving opgezet. Figuur~\ref{fig:ic2-topology} toont de topologie zoals die in InControl2 verschijnt: vier FusionHub-VMs (Bornem, Venue, Live1, Live2) en de fysieke Peplink Balance~20X die als \emph{Live3} is toegevoegd. De VMs draaien in VirtualBox op een Windows-host, verbonden via een VyOS-router die als centrale gateway, NAT en DNAT-port-forward fungeert. Een AlmaLinux~9 VM op \texttt{10.1.1.100} huisvest de volledige observability-stack. Detailinstructies staan in \texttt{poc/omgeving\_opzetten.md}.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.95\linewidth,height=0.42\textheight,keepaspectratio]{ic2-topology-pepvpn.png}
  \caption[Topologie in InControl2]{PepVPN-topologie in InControl2. Vier FusionHub-spokes vormen samen met de hub in Bornem een gedeeltelijke mesh; twee Point-to-Point-profielen verbinden de surgery-sites rechtstreeks met Venue.}
  \label{fig:ic2-topology}
\end{figure}

De gekozen ICS~+~Host-Only~+~VyOS-topologie werd bewust boven VirtualBox' NAT-adapter verkozen, omdat die laatste geen inkomend verkeer toestaat op UDP~4500 (nodig voor PepVPN). De opstelling geeft stabiele, reproduceerbare directe routering ongeacht de locatie van de laptop (thuis, school, hotspot).

\begin{table}[htbp]
  \centering
  \caption[IP-schema en toegang testomgeving]{IP-adressen en webadmin-URL's van de testomgeving.}
  \label{tab:ip-schema}
  \begin{tabular}{l l l l}
    \hline
    \textbf{Component} & \textbf{Intern netwerk} & \textbf{IP} & \textbf{Toegang} \\
    \hline
    VyOS-router         & Host-Only (ICS)  & 192.168.137.10 & SSH \\
    FH-Bornem           & intnet-bornem    & 10.1.1.2       & \texttt{:8441} \\
    FH-Venue            & intnet-venue     & 10.1.2.2       & \texttt{:8442} \\
    FH-Live1            & intnet-surgery1  & 10.1.3.2       & \texttt{:8443} \\
    FH-Live2            & intnet-surgery2  & 10.1.4.2       & \texttt{:8444} \\
    Balance~20X (Live3) & WiFi-direct      & 192.168.1.1    & WiFi \texttt{PEPLINK\_2622} \\
    Observability-VM    & intnet-bornem    & 10.1.1.100     & Grafana \texttt{:3000} \\
    \hline
  \end{tabular}
\end{table}

\section{Observability-stack}
\label{sec:poc-stack}

De stack draait als twaalf Docker-containers op de observability-VM, georkestreerd via Docker Compose. De keuze voor containers garandeert reproduceerbaarheid en isoleert elke functie in een eigen eenheid. Tabel~\ref{tab:containers} geeft een overzicht.

\begin{table}[htbp]
  \centering
  \caption[Containers in de observability-stack]{De twaalf containers van de observability-stack en hun rol.}
  \label{tab:containers}
  \begin{tabular}{l l p{0.54\linewidth}}
    \hline
    \textbf{Container} & \textbf{Poort} & \textbf{Functie} \\
    \hline
    prometheus            & 9090      & Time-series opslag en PromQL-queries \\
    grafana               & 3000      & Dashboards, alerting, provisioning \\
    loki                  & 3100      & Log-aggregatie met 30 dagen retentie \\
    promtail              & 1514/udp  & Syslog-receiver en Docker log-collector \\
    blackbox-exporter     & 9115      & ICMP-probes per site (\texttt{probe\_success}, RTT) \\
    incontrol2-exporter   & 8080      & Custom Python-exporter: InControl2 API + SNMP + lokale API \\
    ping-exporter         & 9116      & Jitter (\texttt{mdev}) en packet loss via \texttt{ping -c 20} \\
    node-exporter         & 9100      & Systeemmetrics van de VM \\
    srt-exporter          & 9117      & Parser van \texttt{srt-live-transmit} CSV-stats \\
    srt-test-stream       & --        & FFmpeg-testsignaal als SRT-caller naar \texttt{srt-exporter} \\
    ndi-exporter          & 9118      & \texttt{ctypes}-binding op de NDI SDK; frame-level metrics per NDI-bron \\
    ndi-test-stream       & --        & Synthetische NDI-bron (SMPTE-kleurbalken 1280$\times$720 @ 25~fps) \\
    \hline
  \end{tabular}
\end{table}

Vijf containers draaien met \texttt{network\_mode: host} omdat ze rechtstreeks moeten kunnen communiceren met netwerkinterfaces van de VM: de \texttt{incontrol2-exporter} voor SNMP- en lokale-API-polling, de \texttt{blackbox-exporter} voor ICMP, de \texttt{ping-exporter} voor raw-sockets, de \texttt{srt-exporter} die een UDP-listener op de loopback opzet, en de \texttt{ndi-exporter} die via mDNS/Avahi NDI-bronnen ontdekt. Een gevolg is dat Prometheus deze exporters adresseert via het VM-IP (\texttt{10.1.1.100}), niet via de containernaam. De SRT- en NDI-teststream-containers staan bovendien achter een Docker-Compose-profile (\texttt{demo}) zodat ze enkel meelopen tijdens scenario-metingen en niet tijdens baseline-resource-metingen.

\subsection{Technische bevindingen tijdens integratie}

Tijdens de implementatie kwamen verschillende praktijkbevindingen naar boven die belangrijk zijn om de reproduceerbaarheid te garanderen. De meest relevante worden hieronder samengevat.

\begin{itemize}
  \item \textbf{pysnmp-versie gepind op 4.4.12.} Recentere \texttt{pysnmp}-versies (v5, v6, v7) hebben een async-API die niet compatibel is met de synchrone \texttt{getCmd}/\texttt{nextCmd}-aanroepen. Enkel \texttt{pysnmp==4.4.12} in combinatie met \texttt{pyasn1==0.4.8} werkt correct in Python~3.11.
  \item \textbf{CPU- en geheugen-OIDs zijn niet beschikbaar op FusionHub.} De Peplink enterprise-OIDs onder \texttt{.1.3.6.1.4.1.23695.200.*} bestaan enkel op fysieke hardware. Dit verschil is een structureel gevolg van het virtuele karakter van FusionHub en wordt in de observability-stack opgevangen door de lokale REST-API te gebruiken: \texttt{status.cpu} werkt op beide platformen en levert de unieke bron voor CPU-belasting.
  \item \textbf{Statische routes vereist op de observability-VM.} Omdat de VM standaard enkel \texttt{10.1.1.0/24} kent, moeten de subnetten van Venue, Live1 en Live2 expliciet via VyOS als gateway toegevoegd worden. Zonder deze routes tonen de overige sites ICMP~DOWN in Grafana.
  \item \textbf{Docker Compose leest \texttt{.env} niet opnieuw bij restart.} Een klassieke valkuil: \texttt{docker compose restart} behoudt de oude environment. Enkel \texttt{docker compose up -d --force-recreate} past een gewijzigde \texttt{.env} effectief toe.
  \item \textbf{NDI-discovery vereist een bereikbare Avahi-daemon, niet enkel \texttt{libavahi-client}.} Het NDI-protocol gebruikt mDNS (\texttt{\_ndi.\_tcp.local.}) om bronnen te ontdekken. \texttt{libavahi-client3} in de container installeren is niet voldoende --- die library heeft een dráaiende \texttt{avahi-daemon} nodig om mee te praten, anders geeft \texttt{NDIlib\_find\_get\_current\_sources()} nul bronnen (óók met \texttt{NDI\_EXTRA\_IPS=127.0.0.1}, omdat die parameter alsnog mDNS-resolutie vereist). Opgelost door \texttt{avahi} op de observability-VM te installeren en de DBus- en Avahi-socket van de host via bind-mounts door te geven aan beide NDI-containers.
  \item \textbf{NDI-exporter moet gauges expliciet op nul zetten bij offline-stream.} Tijdens scenario~6 viel op dat \texttt{ndi\_video\_fps} op 25 bleef hangen nadat de sender was gestopt, hoewel \texttt{ndi\_stream\_active} correct naar~0 ging. De oorzaak was dat de \emph{``geen bron''}-branch van de exporter enkele gauges niet resette, waardoor Prometheus de laatste waarde behield tot de scrape zelf stale werd. De fix zet \texttt{video\_fps}, \texttt{queue\_depth}, \texttt{video\_width/height} en \texttt{frame\_drop\_rate} expliciet op~0 zodra de target verdwijnt, en reset de interne frame-counters om een counter-sprong bij recovery te vermijden. Dit is hetzelfde \emph{default-to-healthy}-patroon als de blindspot-bug uit sectie~\ref{sec:poc-blindspot} en onderstreept dat \emph{``geen data''} nooit impliciet als \emph{``alles is oké''} mag gelden.
  \item \textbf{NDI-receivers moeten continu gedraind worden.} Een eerste versie van de \texttt{ndi-exporter} riep \texttt{NDIlib\_recv\_capture\_v2()} één keer per scrape-interval aan. Daardoor liep de interne NDI-queue vol en werd ongeveer de helft van de binnenkomende frames weggegooid, zichtbaar als een kunstmatige \texttt{frame\_drop\_rate} van ca.\ 50\%. De uiteindelijke implementatie gebruikt een tight drain-loop met een 5~ms-timeout die continu frames ophaalt; de performance- en queue-statistieken worden apart uitgelezen. De restdrop-rate in loopback-situatie (\textasciitilde{}10--15\%) is een eigenschap van de synthetische Python-sender en blijft in productie afwezig wanneer een hardware-encoder de bron levert.
\end{itemize}

\section{Datacollectie en verzamelde metrics}
\label{sec:poc-metrics}

De stack combineert vier databronnen die elkaar complementeren. Elke laag dekt een eigen faaldomein af, wat noodzakelijk is om te kunnen onderscheiden of een probleem zich situeert in de cloudlaag, de netwerkverbinding, het toestel zelf of de applicatie.

\begin{description}
  \item[InControl2 API.] De cloud van Peplink wordt elke 15 seconden gepolld via een OAuth2-token. De \texttt{incontrol2-exporter} exporteert hieruit \texttt{peplink\_device\_online}, \texttt{peplink\_device\_uptime\_seconds}, \texttt{peplink\_device\_client\_count}, \texttt{peplink\_device\_tx\_bytes}, \texttt{peplink\_device\_rx\_bytes}, \texttt{peplink\_tunnel\_up}, \texttt{peplink\_tunnel\_count} en \texttt{peplink\_recent\_event\_count}.
  \item[SNMP.] Directe SNMP-polling levert MIB-II-waardes (\texttt{peplink\_snmp\_reachable}, uptime, response-tijd, interface-bytes) voor alle vijf de sites. Op de fysieke Balance~20X komen daar meer dan 200 Peplink enterprise-OIDs bij, waaronder \texttt{peplink\_snmp\_wan\_status}, \texttt{peplink\_snmp\_wan\_link\_up}, \texttt{peplink\_snmp\_wan\_signal\_dbm}, \texttt{peplink\_snmp\_wan\_healthcheck} en \texttt{peplink\_snmp\_wifi\_client\_count}.
  \item[Lokale Peplink REST-API.] Via het endpoint \texttt{/cgi-bin/MANGA/api.cgi} wordt \texttt{status.cpu} en \texttt{status.ap} opgehaald; die leveren respectievelijk \texttt{peplink\_local\_cpu\_load\_percent} en \texttt{peplink\_local\_ap\_up}. Een metric \texttt{peplink\_local\_api\_reachable} fungeert als zelfbewaking van deze bron.
  \item[ICMP en jitter.] De blackbox-exporter levert \texttt{probe\_success} en \texttt{probe\_icmp\_duration\_seconds}, terwijl de ping-exporter via \texttt{ping -c 20} nauwkeurige waardes voor \texttt{ping\_jitter\_ms}, \texttt{ping\_rtt\_avg\_ms} en \texttt{ping\_packet\_loss\_percent} berekent. Jitter via \texttt{mdev} is substantieel nauwkeuriger dan een PromQL-benadering met \texttt{stddev\_over\_time}.
  \item[SRT-stream.] De \texttt{srt-exporter} start \texttt{srt-live-transmit} als listener op UDP~9000 en parsed de CSV-statistieken. Dat levert \texttt{srt\_stream\_active}, \texttt{srt\_bitrate\_kbps}, \texttt{srt\_rtt\_ms}, \texttt{srt\_packet\_loss\_percent}, \texttt{srt\_jitter\_ms}, \texttt{srt\_retransmit\_rate} en \texttt{srt\_retransmit\_total}.
  \item[NDI-stream.] De \texttt{ndi-exporter} spreekt via een \texttt{ctypes}-binding rechtstreeks met \texttt{libndi.so.6} uit het NDI SDK. Hij abonneert zich op een bronnaam (standaard \emph{``NDI Test Stream''}) en levert \texttt{ndi\_stream\_active}, \texttt{ndi\_frames\_received\_total}, \texttt{ndi\_frames\_dropped\_total}, \texttt{ndi\_frame\_drop\_rate}, \texttt{ndi\_video\_fps}, \texttt{ndi\_video\_width}, \texttt{ndi\_video\_height} en \texttt{ndi\_queue\_depth\_frames}. Daarnaast publiceert hij \texttt{ndi\_sdk\_available} en \texttt{ndi\_sources\_detected} als zelf-bewaking: als de SDK ontbreekt schakelt de exporter automatisch over op een discovery-only modus op basis van zeroconf.
  \item[Logs.] FusionHub- en Peplink-syslog wordt via UDP~1514 naar Promtail verstuurd en opgeslagen in Loki, samen met de \texttt{stdout}-logs van de containers zelf.
\end{description}

In totaal leveren de exporters meer dan 35 metrics per scrape, wat toelaat om gelijktijdige afhankelijkheden (bijvoorbeeld de correlatie tussen WAN-packet-loss en SRT-retransmits) direct in Grafana te visualiseren.

\section{Grafana-dashboard}
\label{sec:poc-dashboard}

Het centrale dashboard \emph{Mediaventures --- Observability Dashboard} is opgebouwd uit twaalf secties die van boven naar beneden de volledige keten tonen. Het dashboard wordt via JSON-provisioning geladen, zodat elke wijziging versiebeheerbaar is en niet verloren gaat bij een restart.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\linewidth,height=0.55\textheight,keepaspectratio]{5-baseline-alles.png}
  \caption[Dashboard baseline (volledig overzicht)]{Baseline van het volledige dashboard onder normale werking: alle vijf de sites rapporteren groen en alle metrics bevinden zich binnen de ingestelde drempels.}
  \label{fig:baseline-alles}
\end{figure}

\paragraph{Sectie 1 --- Site Status Overzicht.} Per site tonen drie stat-panels de bereikbaarheid (ICMP), de tunnel- of SNMP-status en de cloud- of lokale-API-status. Dit geeft techniekers in één blik een go/no-go voor elk van de vijf sites (figuur~\ref{fig:baseline-sitestatus}).

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.95\linewidth,height=0.25\textheight,keepaspectratio]{5-baseline-sitestatus.png}
  \caption[Sectie 1: Site Status Overzicht]{Sectie~1 toont per site drie onafhankelijke gezondheidsindicatoren: ICMP, SNMP/tunnel en API.}
  \label{fig:baseline-sitestatus}
\end{figure}

\paragraph{Sectie 2 --- Connectiviteit en netwerkkwaliteit.} Deze sectie combineert ICMP-RTT, packet loss en jitter voor alle sites. De drempelwaarden zijn afgestemd op live-streaming: groen onder 50~ms RTT, rood boven 150~ms; packet loss groen bij 0\%, rood boven 5\%; jitter groen onder 5~ms.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.95\linewidth,height=0.30\textheight,keepaspectratio]{5-baseline-connectiviteit.png}
  \caption[Sectie 2: Connectiviteit en netwerkkwaliteit]{RTT, packet loss en jitter per site. Baseline op het lokale testnetwerk: RTT\,\textless\,2~ms, 0\% loss.}
  \label{fig:baseline-connectiviteit}
\end{figure}

\paragraph{Secties 3 en 4 --- WAN en device health.} Sectie~3 is gewijd aan de enterprise-SNMP van de fysieke Balance~20X (WAN-interface-status, link-up, signaal in dBm, health check). Sectie~4 toont de CPU-load en AP-status voor alle sites die via de lokale API bereikbaar zijn. Samen leveren ze het bewijs dat de stack ook op fysieke Peplink-hardware werkt, met metrics die FusionHub niet aanbiedt.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.95\linewidth,height=0.38\textheight,keepaspectratio]{5-baseline-3-4-wanendevicehealth.png}
  \caption[Secties 3 en 4: WAN status en device health]{Enterprise SNMP WAN-interfaces van Live3 (Balance~20X) en de CPU-load gemeten via de lokale REST-API.}
  \label{fig:baseline-wan-health}
\end{figure}

\paragraph{Sectie 5 --- PepVPN-tunnels.} Per actief SpeedFusion-profiel tonen stat-panels de tunnelstatus; een tijdreeks geeft drops en herstel weer. Na de fix die besproken wordt in sectie~\ref{sec:poc-blindspot} bevat deze sectie ook een expliciete \texttt{peplink\_tunnel\_count}-indicator.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.95\linewidth,height=0.28\textheight,keepaspectratio]{5-baseline-pepvpntunnels.png}
  \caption[Sectie 5: PepVPN-tunnels]{Drie actieve SpeedFusion-profielen (Bornem-hub, Live1$\leftrightarrow$Venue, Live2$\leftrightarrow$Venue).}
  \label{fig:baseline-pepvpn}
\end{figure}

\paragraph{Secties 6--10.} Sectie~6 bundelt de InControl2-data (uptime, clients, bandbreedte); sectie~7 toont directe SNMP-metrics die sneller reageren dan de cloud-API; sectie~8 bevat het Loki log-paneel (FusionHub-syslog en Docker-logs); sectie~9 toont de event-teller per device; sectie~10 is een \emph{meta-paneel} dat de gezondheid van de stack zelf bewaakt (\texttt{peplink\_scrape\_success}, scrape-duur, teller van API/SNMP/lokale-API-fouten).

\paragraph{Sectie 11 --- SRT stream kwaliteit.} Acht panels bewaken de live SRT-stream: stream-active, bitrate, RTT, packet loss, jitter, retransmit-rate, de totale retransmissions en een \emph{cross-layer} panel dat de SRT-RTT samen toont met de ICMP-RTT per site. Dat cross-layer-panel is een kernbijdrage: gelijktijdige pieken in beide signalen bewijzen dat een netwerkverstoring direct doorwerkt op de streamkwaliteit.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.95\linewidth,height=0.32\textheight,keepaspectratio]{5-baseline-srt.png}
  \caption[Sectie 11: SRT stream kwaliteit]{Baseline van de SRT-teststream: \textasciitilde{}1\,695 kbps, RTT $\approx$ 1~ms op loopback, 0\% loss.}
  \label{fig:baseline-srt}
\end{figure}

\paragraph{Sectie 12 --- NDI stream kwaliteit.} Analoog aan sectie~11 voor SRT bevat deze sectie stat- en gauge-panels voor \texttt{ndi\_stream\_active}, \texttt{ndi\_video\_fps}, \texttt{ndi\_frame\_drop\_rate}, \texttt{ndi\_sources\_detected} en \texttt{ndi\_sdk\_available}, aangevuld met tijdreeksen voor de framerate, de receiver-queue-diepte, het aantal ontvangen versus gedropte frames per seconde, en de drop-rate doorheen de tijd. Het toevoegen van deze sectie bewijst dat dezelfde observability-aanpak die voor SRT werkt (één exporter die de applicatielaag vertaalt naar Prometheus-metrics) ook op het dominante IP-videoprotocol binnen productie-LANs van Mediaventures van toepassing is. De \texttt{queue\_depth}-metric is bijzonder waardevol: een stijgende bufferbezetting vormt een vroeg signaal van congestie of receiver-saturatie, nog vóór frame drops zich manifesteren.

\section{Alerting}
\label{sec:poc-alerting}

Het PoC bevat tien provisioned alert rules in \texttt{alerts.yml}, elk gericht op een specifiek faaldomein. Tabel~\ref{tab:alerts} vat de regels samen.

\begin{table}[htbp]
  \centering
  \caption[Provisioned alert rules]{Overzicht van de tien alert rules in Grafana, gegroepeerd per faaldomein.}
  \label{tab:alerts}
  \begin{tabular}{l l l p{0.40\linewidth}}
    \hline
    \textbf{Alert} & \textbf{Sev.} & \textbf{For} & \textbf{Trigger} \\
    \hline
    Device Offline                   & CRIT & 1~min & \texttt{(1 - peplink\_device\_online) > 0} \\
    PepVPN Tunnel Down               & CRIT & 1~min & \texttt{(1 - peplink\_tunnel\_up) > 0} \\
    PepVPN No Profiles Configured    & WARN & 5~min & \texttt{max(peplink\_tunnel\_count) == 0} \\
    Site onbereikbaar (ICMP)         & WARN & 2~min & \texttt{(1 - probe\_success) > 0} \\
    Hoge VPN-latency                 & WARN & 5~min & RTT $>$ 150~ms \\
    Hoog packet loss                 & WARN & 5~min & packet loss $>$ 5\% \\
    WAN Link Down                    & WARN & 2~min & \texttt{(1 - peplink\_snmp\_wan\_link\_up) > 0} \\
    Hoge CPU Load                    & WARN & 5~min & \texttt{peplink\_local\_cpu\_load\_percent > 85} \\
    Exporter Down                    & CRIT & 2~min & \texttt{(1 - peplink\_scrape\_success) > 0} \\
    SRT Stream Packet Loss           & WARN & 1~min & \texttt{srt\_packet\_loss\_percent > 5} \\
    \hline
  \end{tabular}
\end{table}

\paragraph{Syntactische keuze: \texttt{(1 - X) > 0}.} Grafana Unified Alerting behandelt een waarde van~0 in bepaalde contexten als "geen data", waardoor naïeve \texttt{== 0}-queries nooit firen. Alle regels gebruiken daarom de formulering \texttt{(1 - X) > 0}, zodat een waarde van~0 wél als alarmerend wordt herkend.

\paragraph{Verantwoording \texttt{noDataState: OK}.} Alle regels gebruiken expliciet \texttt{noDataState: OK}. De keuze is bewust en berust op vier argumenten. Ten eerste zou \texttt{noDataState: Alerting} alle tien regels gelijktijdig laten vuren bij een enkele gemiste scrape, wat onvermijdelijk leidt tot alert-fatigue. Ten tweede is er een aparte \texttt{Exporter Down}-regel die de scrape-gezondheid centraal bewaakt; elke andere regel kan dus rustig op \texttt{OK} staan omdat "geen data" al elders wordt gedetecteerd. Ten derde respecteert dit het principe dat elke regel één faaldomein monitort in plaats van hun eigen dataverlies apart te signaleren. Ten vierde is in een live AV-productie de kost van een valse alarm (techniekers die onterecht gewekt worden) hoger dan de kost van een detectie-vertraging van hooguit twee minuten, die bovendien door de \texttt{Exporter Down}-regel hard begrensd is.

\section{Testscenario's en validatie}
\label{sec:poc-tests}

Om aan te tonen dat de stack storingen effectief detecteert, werd een reeks testscenario's uitgevoerd die de netwerk-, stream- en NAT-laag afdekken. De netwerk-scenario's worden gesimuleerd met \texttt{tc netem} op VyOS; de SRT-scenario's op de loopback-interface van de observability-VM (omdat de SRT-containers met \texttt{network\_mode: host} draaien en hun verkeer niet via een Docker-bridge loopt). Een volledige handleiding, inclusief de exacte \texttt{tc}-commando's, staat in \texttt{poc/testscenarios.md}.

\subsection{Scenario 2a --- packet loss 10\% op Live1}

Op \texttt{eth3} (Live1-interface) wordt 10\% packet loss ingevoerd via \texttt{tc qdisc add dev eth3 root netem loss 10\%}. Binnen de minuut stijgt \texttt{ping\_packet\_loss\_percent} voor Live1 naar de ingestelde waarde; na vijf minuten gaat de alert \emph{Hoog packet loss} naar Firing (figuur~\ref{fig:scenario-2a}). De cross-check via ICMP en via de jitter-exporter komt consistent overeen.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.95\linewidth,height=0.30\textheight,keepaspectratio]{scenario_2a_packetloss_dashboard.png}
  \caption[Scenario 2a: packet loss]{Packet loss van 10\% zichtbaar op Live1; de alert gaat via Pending naar Firing.}
  \label{fig:scenario-2a}
\end{figure}

\subsection{Scenario 2b --- 200 ms latency op Venue}

Op \texttt{eth2} wordt 200~ms vertraging toegevoegd. De gemeten RTT stijgt naar ongeveer 400~ms (heen-en-terug), en de alert \emph{Hoge VPN-latency} firet na vijf minuten. Het dashboard toont duidelijk de sprong in de tijdreeks (figuur~\ref{fig:scenario-2b}).

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.95\linewidth,height=0.30\textheight,keepaspectratio]{scenario_2b_latency_dashboard.png}
  \caption[Scenario 2b: 200 ms latency]{Latency-injectie op de Venue-interface: RTT verdubbelt en de alert gaat naar Firing.}
  \label{fig:scenario-2b}
\end{figure}

\subsection{Scenario 2c --- volledige linkdown op Live1}

Door \texttt{ip link set eth3 down} valt Live1 volledig weg. Binnen twee minuten firet de alert \emph{Site onbereikbaar (ICMP)}; de InControl2-timeout is trager (enkele minuten), waardoor \texttt{peplink\_device\_online} pas later op~0 komt te staan. Figuur~\ref{fig:scenario-2c} toont beide alerts tegelijk in Grafana.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.95\linewidth,height=0.28\textheight,keepaspectratio]{scenario_2d_linkdown_dashboard.png}
  \caption[Scenario 2c: link down]{Live1 wordt volledig van het netwerk losgekoppeld; meerdere alerts firen gelijktijdig.}
  \label{fig:scenario-2c}
\end{figure}

\subsection{Scenario 3b --- meervoudige gelijktijdige storingen}

Op drie interfaces wordt tegelijk degradatie geïnjecteerd: Venue krijgt 150~ms~+~2\% loss, Live1 100~ms~+~5\% loss en Live2 80~ms~+~3\% loss. Het dashboard toont onmiddellijk welke sites het zwaarst getroffen zijn en de bijbehorende alerts firen parallel (figuur~\ref{fig:scenario-3b}). Dit bewijst dat de dashboards schalen naar een incident dat meerdere locaties tegelijk raakt --- een realistisch scenario tijdens een internationaal congres.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.95\linewidth,height=0.30\textheight,keepaspectratio]{scenario_3b_multi_dashboard.png}
  \caption[Scenario 3b: meervoudige storingen]{Drie sites degraderen tegelijk; Grafana rangschikt ze visueel van zwaarst tot lichtst getroffen.}
  \label{fig:scenario-3b}
\end{figure}

\subsection{Scenario 4 --- SRT onder druk}

\paragraph{4a: 30\% packet loss op UDP:9000.} De netem-regel wordt op de loopback-interface geplaatst met een \texttt{u32}-filter op poort~9000. Verrassend genoeg toont \texttt{srt\_packet\_loss\_percent} slechts 4~tot 10\% gemeten verlies, ondanks de 30\% geïnjecteerde loss. De verklaring is SRT's ingebouwde Automatic Repeat Request (ARQ): verloren pakketten worden binnen de latency-buffer opnieuw verzonden, waardoor de eindgebruiker ze niet ziet. De werkelijke netwerkbelasting wordt zichtbaar via \texttt{srt\_retransmit\_rate} en \texttt{srt\_retransmit\_total}, die beide sterk oplopen. Bij langdurige simulatie stijgt het gemeten verlies progressief door een ARQ-cascade: retransmissions zijn zelf ook onderhevig aan de 30\% loss (figuur~\ref{fig:scenario-4a}).

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.95\linewidth,height=0.30\textheight,keepaspectratio]{scenario_4a_srt_packetloss_peak.png}
  \caption[Scenario 4a: SRT packet loss]{30\% netem loss op UDP:9000. \texttt{srt\_retransmit\_rate} stijgt sterk, terwijl \texttt{srt\_packet\_loss\_percent} pas na tijd progressief oploopt door de ARQ-cascade.}
  \label{fig:scenario-4a}
\end{figure}

\paragraph{4b: 100 ms latency.} Een netem-delay van 100~ms benadert de standaard SRT-latency-buffer van 120~ms. Pakketten die te laat aankomen worden door SRT als verloren beschouwd, ook al zijn ze niet technisch gedropped. Het resultaat is een paradox: \emph{zonder} echte packet loss meet SRT toch ongeveer 35\% verlies. Figuur~\ref{fig:scenario-4b-overview} toont de volledige tijdlijn inclusief het herstel nadat de \texttt{tc}-regel verwijderd werd. Deze test bewijst het belang van RTT-monitoring naast packet loss: op SRT-niveau hebben beide effectief hetzelfde gevolg.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.95\linewidth,height=0.30\textheight,keepaspectratio]{scenario_4b_srt_latency_overview.png}
  \caption[Scenario 4b: SRT latency]{100~ms delay veroorzaakt \textasciitilde{}35\% SRT packet loss doordat pakketten buiten de latency-buffer aankomen.}
  \label{fig:scenario-4b-overview}
\end{figure}

\paragraph{Gemeten time-to-detect.} Voor de SRT packet-loss-alert werd een uurwerkmeting uitgevoerd: T0 om 19:47:51 (tc netem actief), T+31~s eerste scrape boven drempel, T+118~s (19:49:49) Grafana-alert gefired. De totale time-to-detect is dus ongeveer twee minuten. Dat is substantieel sneller dan de huidige werkwijze, waarbij techniekers pas reageren op basis van klachten van de kijker.

\subsection{Scenario 5 --- fysieke Balance 20X offline}

Het laatste scenario valideert dat de stack ook op fysieke hardware werkt. De WiFi-AP van de Balance~20X wordt uitgeschakeld, waardoor de observability-VM haar route naar \texttt{192.168.1.1} verliest. Drie onafhankelijke kanalen (ICMP, SNMP enterprise, lokale API) slaan tegelijk alarm (figuur~\ref{fig:scenario-5a}); na ongeveer twee minuten firen de bijhorende alerts.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.95\linewidth,height=0.28\textheight,keepaspectratio]{scenario_5a_live3_offline_status.png}
  \caption[Scenario 5a: Live3 offline]{Fysieke Balance~20X offline: ICMP, SNMP en lokale API rapporteren onafhankelijk DOWN.}
  \label{fig:scenario-5a}
\end{figure}

Een interessant neveneffect trad op: de WiFi-verbinding verzorgde ook de internettoegang van de observability-VM, waardoor de \texttt{incontrol2-exporter} tijdelijk zijn DNS-resolutie naar de InControl2-cloud verloor. Dat triggerde indirect ook de \emph{PepVPN Tunnel Down}-alert voor de overige sites --- een realistisch productie-inzicht: een monitoringhost die zijn eigen connectiviteit verliest, kan false positives genereren voor andere locaties. De aanbeveling voor productie is dan ook om de monitoringhost via een redundante uplink te voorzien.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.95\linewidth,height=0.26\textheight,keepaspectratio]{scenario_5b_live3_herstel.png}
  \caption[Scenario 5b: herstel]{Na het heractiveren van de WiFi-AP herverbindt de VM en komen alle panels binnen 1--2 minuten opnieuw op UP.}
  \label{fig:scenario-5b}
\end{figure}

\subsection{Scenario 6 --- NDI-stream onderbreking}
\label{sec:scenario-6}

Dit scenario is het NDI-analogon van scenario~4 (SRT) en valideert dat de stack een plotse uitval van een NDI-bron binnen seconden detecteert en alarmeert. NDI-monitoring verloopt volledig via de \texttt{ndi-exporter} (een ctypes-binding op NDI SDK~v6) met als bron de synthetische \texttt{ndi-test-stream} (Python-sender, SMPTE-kleurbalken 1280$\times$720 @ 25~fps). Discovery gebeurt via Avahi/mDNS (\texttt{\_ndi.\_tcp.local.}).

\paragraph{6a --- stream offline.} Na het stoppen van de sender (\texttt{docker compose --profile demo stop ndi-test-stream}) zakt \texttt{ndi\_stream\_active} binnen één scrape-interval (~15~s) naar 0, verdwijnt de mDNS-advertisement en daalt \texttt{ndi\_sources\_detected} naar 0. De bijhorende alert \emph{[CRITICAL] NDI Stream Inactief} gaat na de \texttt{for}-duur van één minuut in Firing (figuur~\ref{fig:scenario-6a-alert}), en sectie~12 van het dashboard kleurt volledig rood met alle timeseries op nul (figuur~\ref{fig:scenario-6a-outage}).

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.95\linewidth,height=0.26\textheight,keepaspectratio]{scenario_6a_ndi_outage.png}
  \caption[Scenario 6a: NDI outage]{NDI-bron gestopt: stream inactive, bronnen 0, framerate 0, timeseries op flatline.}
  \label{fig:scenario-6a-outage}
\end{figure}

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.95\linewidth,height=0.22\textheight,keepaspectratio]{scenario_6a_ndi_alert_firing.png}
  \caption[Scenario 6a: alert firing]{De alertregel \emph{[CRITICAL] NDI Stream Inactief} in Firing-state na de \texttt{for}-duur van 1~minuut.}
  \label{fig:scenario-6a-alert}
\end{figure}

\paragraph{Gauge-staleness: een subtiele bug die pas tijdens de test opviel.} Bij de eerste uitvoering bleef het stat-panel \emph{Framerate} op 25~fps staan, terwijl \emph{Stream Active} wél op OFFLINE sprong (figuur~\ref{fig:scenario-6a-staleness}). De oorzaak: de exporter resette in de \emph{"geen bron"}-branch enkele gauges niet, waardoor Prometheus de laatste waarde behield tot de scrape zelf stale werd. De fix zet \texttt{ndi\_video\_fps}, \texttt{ndi\_queue\_depth\_frames}, \texttt{ndi\_video\_width/height} en \texttt{ndi\_frame\_drop\_rate} expliciet op nul wanneer de target verdwijnt, en reset de interne frame-counters om counter-sprongen bij recovery te vermijden. Dit is een algemeen Prometheus-gauge-antipattern en wordt samen met de \emph{default-to-healthy}-les uit sectie~\ref{sec:poc-blindspot} meegenomen als ontwerpaanbeveling.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.95\linewidth,height=0.22\textheight,keepaspectratio]{scenario_6a_ndi_staleness_before_fix.png}
  \caption[Scenario 6a: gauge-staleness vóór fix]{Eerste uitvoering: Stream Active staat op OFFLINE, maar Framerate blijft op 25~fps hangen --- bewijs van de gauge-staleness-bug die de fix in \texttt{ndi\_exporter.py} motiveerde.}
  \label{fig:scenario-6a-staleness}
\end{figure}

\paragraph{6b --- recovery.} Na \texttt{docker compose --profile demo start ndi-test-stream} verschijnt de mDNS-advertisement binnen enkele seconden opnieuw. \texttt{ndi\_stream\_active} springt terug naar 1, de framerate stabiliseert op 25~fps en de frame-drop-rate stabiliseert op de baseline van ~10--15\% die inherent is aan de Python-sender (in productie met hardware-encoder verdwijnt deze baseline grotendeels). De timeseries in figuur~\ref{fig:scenario-6b} tonen een duidelijke stapfunctie van 0 naar 25~fps --- visueel bewijs van de recovery en van de correctheid van de counter-reset in de staleness-fix.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.95\linewidth,height=0.26\textheight,keepaspectratio]{scenario_6b_ndi_recovery.png}
  \caption[Scenario 6b: NDI recovery]{Recovery na herstart van de sender: duidelijke stapfunctie 0$\rightarrow$25~fps in de timeseries, alert zakt terug naar Normal.}
  \label{fig:scenario-6b}
\end{figure}

\paragraph{Detectietijd.} De gemeten time-to-detect bedraagt ongeveer 15~s voor de metric-zelf (één scrape-interval) en 1~min~15~s tot de alert in Firing-state gaat. Dat is in dezelfde orde van grootte als de SRT-alert (scenario~4a, ~2~min), en ruim snel genoeg om tijdens een uitzending in te grijpen vóór de klacht van de eindgebruiker arriveert.

\subsection{Scenario 7 --- asymmetrische NAT (NAT-traversal validatie)}
\label{sec:scenario-7}

De methodologie (sectie~\ref{sec:fase5}) benoemt drie connectiviteitsscenarios als productcontext voor PepVPN: publiek IP, één NAT, en dubbele NAT. De basisopstelling van de virtuele testomgeving gebruikt directe routering via VyOS zonder NAT tussen de spokes en test geen van die drie empirisch. Scenario~7 voegt één expliciet asymmetrisch NAT-geval toe om de kernclaim van de observability-laag te valideren: zolang \texttt{peplink\_tunnel\_up=1} blijft de stack \emph{routing-agnostisch}, omdat alle exporters op tunneladressen (\texttt{10.1.x.x}) werken, ongeacht hoeveel NAT-lagen onder de tunnel zitten. Wat dit scenario expliciet \emph{niet} bewijst, is PepVPN's eigen NAT-T-implementatie onder CGNAT of hole-punching-gedrag --- dat is commerciële productfunctionaliteit van Peplink en valt buiten de observability-scope.

\paragraph{Opstelling.} Eén extra \texttt{source NAT}-regel (rule~310) op VyOS masquereert verkeer van FH-Live1 (\texttt{10.1.3.0/24}) richting het Bornem-subnet (\texttt{10.1.1.0/24}) via interface \texttt{eth1}. Vanuit FH-Bornem's perspectief lijkt Live1 plots afkomstig van \texttt{10.1.1.1} (VyOS-eth1) in plaats van \texttt{10.1.3.2} --- asymmetrische NAT. De overige P2P-tunnel (Live1$\leftrightarrow$Venue) blijft ongewijzigd.

\paragraph{Uitvoering.} Na het committen van rule~310 bleef de counter initieel op nul staan: de bestaande Bornem$\leftrightarrow$Live1 PepVPN-tunnel was oorspronkelijk door Bornem geïnitieerd, waardoor de conntrack-entry in de Bornem$\rightarrow$Live1-richting stond en de SNAT-regel niet raakte. Pas na het flushen van die entry (\texttt{sudo conntrack -D -p udp --sport 4500 --dport 4500 ...}) initieerde Live1 een verse outbound flow die wél onder rule~310 valt. In productie treedt dit artefact niet op, omdat de spoke-router (Live1) standaard outbound initieert naar een hub met publiek IP.

\paragraph{Bevindingen.} Na de flush groeide de nft-counter van rule~310 continu door (figuur~\ref{fig:scenario-7-nft}), en een \texttt{conntrack}-dump toont de actieve masquerade-binding waarbij UDP~4500 (PepVPN transport) wordt vertaald naar een ephemeral poort (figuur~\ref{fig:scenario-7-conntrack}, \texttt{dport=24249} in de reply-tuple = SNAT in actie).

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.95\linewidth,height=0.22\textheight,keepaspectratio]{scenario_7_vyos_nat_rule.png}
  \caption[Scenario 7: VyOS nft counter]{\texttt{nft list chain ip vyos\_nat POSTROUTING}: rule~310 telt actief verkeer van \texttt{10.1.3.0/24} naar \texttt{10.1.1.0/24}, bewijs dat de SNAT-masquerade werkelijk geraakt wordt.}
  \label{fig:scenario-7-nft}
\end{figure}

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.95\linewidth,height=0.22\textheight,keepaspectratio]{scenario_7_vyos_conntrack.png}
  \caption[Scenario 7: conntrack masquerade-entry]{\texttt{conntrack -L} toont de bidirectionele UDP~4500-flow tussen \texttt{10.1.1.2} en \texttt{10.1.3.2}; de reply-tuple met \texttt{dport=24249} (in plaats van 4500) bewijst dat VyOS het bron-IP en de bronpoort masquereert.}
  \label{fig:scenario-7-conntrack}
\end{figure}

Ondanks de actieve masquerade op de transport-laag bleef de observability-stack ongewijzigd rapporteren. Tabel~\ref{tab:scenario-7} vat de metingen samen: \texttt{peplink\_tunnel\_up}, \texttt{probe\_success} en \texttt{peplink\_device\_online} voor Live1 bleven continu op~1, geen enkele alert firede, en de tunnel vertoonde geen zichtbare flap op scrape-resolutie (15~s). Indien er al een korte rebinding-flap optrad, was die korter dan de alert-drempel van één minuut.

\begin{table}[htbp]
  \centering
  \caption[Metingen scenario 7]{Prometheus-metingen voor, tijdens en na het activeren van rule~310.}
  \label{tab:scenario-7}
  \begin{tabular}{l c c c}
    \hline
    \textbf{Metric} & \textbf{Voor} & \textbf{Tijdens} & \textbf{Na 60~s} \\
    \hline
    \texttt{peplink\_tunnel\_up\{Live1\}}     & 1 & 1 & 1 \\
    \texttt{peplink\_tunnel\_up\{Bornem\}}    & 1 & 1 & 1 \\
    \texttt{probe\_success\{Live1\}}          & 1 & 1 & 1 \\
    \texttt{peplink\_device\_online\{Live1\}} & 1 & 1 & 1 \\
    Firing alerts                               & 0 & 0 & 0 \\
    Rule~310 counter                           & 0~pkt & 2~pkt & \textgreater 3~pkt (groeit) \\
    \hline
  \end{tabular}
\end{table}

Een aanvullende verificatie via de FH-Bornem webadmin (Status~$\rightarrow$~SpeedFusion VPN) toont dat de peer-IP voor \texttt{conn\_to\_Live1} nog steeds \texttt{10.1.3.2} is, niet \texttt{10.1.1.1}. Dat is correct gedrag: PepVPN registreert de \emph{inner} tunnel-endpoint die tijdens de InControl2-handshake is onderhandeld, niet het \emph{transport}-laag bron-IP dat door VyOS SNAT wordt gemasqueerd (UDP~4500 outer header). PepVPN identificeert peers via serial-number, waardoor de transport-laag masquerade transparant is voor de applicatielaag. Het bewijs van de actieve masquerade zit dus in VyOS-kernelstate (nft-counter en conntrack-entry), niet in de PepVPN-UI.

\paragraph{Conclusie.} Scenario~7 bevestigt dat de observability-laag routing-agnostisch is. Ondanks de SNAT-masquerade op de transport-laag blijven alle drie onafhankelijke monitoringsbronnen --- ICMP-probes via blackbox-exporter, de InControl2-API en de PepVPN-tunnelstatus --- correct rapporteren dat Live1 gezond is. PepVPN's serial-based peer-identificatie maakt het ontwerp inherent NAT-tolerant, en omdat de stack exclusief op tunneladressen (\texttt{10.1.x.x}) werkt, is ze niet afhankelijk van een specifiek transport-IP. Rule~310 blijft als permanente uitbreiding van de PoC-baseline actief: scenario~7 wordt daarmee de nieuwe \emph{default-state} voor Live1, wat de bachproef aantoonbaar robuuster maakt tegenover realistische NAT-topologieën.

\section{Zelf-validatie van de stack}
\label{sec:poc-blindspot}

Een observability-stack die zijn eigen correctheid niet kan bewijzen, is zelf een risico. Tijdens de Live3-integratie op 15 april werd ontdekt dat sectie~5 \emph{alle tunnels als UP} rapporteerde, terwijl er in InControl2 \emph{geen enkel SpeedFusion-profiel} geconfigureerd was. De root cause zat in de originele \texttt{get\_tunnel\_stat}-functie, die een lege lijst (geen tunnels) als \texttt{True} (alles oké) interpreteerde. Dit is een klassieke \emph{default-to-healthy}-antipattern die \textcite{VanRenterghem2025} ook in productiemonitoring ziet terugkeren.

\paragraph{Fix in vier lagen.} De oplossing is bewust gelaagd opgebouwd om regressies te voorkomen:
\begin{enumerate}
  \item \textbf{Exporter-semantiek.} De functie geeft voortaan een tuple \texttt{(count, all\_ok)} terug. Wanneer \texttt{count == 0} wordt \texttt{peplink\_tunnel\_up} expliciet op~0 gezet in plaats van impliciet op~1 te blijven staan.
  \item \textbf{Staleness.} Aan het begin van elke scrape-cyclus roepen de gauges \texttt{.clear()} aan, zodat verdwenen devices ook uit Prometheus verdwijnen.
  \item \textbf{Dashboard-verdediging.} Een nieuwe metric \texttt{peplink\_tunnel\_count} wordt in sectie~5 getoond. Een panel dat 0 profielen én "up" zou tonen is onmiddellijk visueel herkenbaar als een anomalie.
  \item \textbf{Alerting.} De regel \emph{PepVPN No Profiles Configured} firet binnen 5 minuten zodra \texttt{max(peplink\_tunnel\_count) == 0}. Had deze regel vanaf het begin bestaan, dan was de blind-spot op 7 april ontdekt.
\end{enumerate}

De algemene les is generaliseerbaar en wordt in Hoofdstuk~\ref{ch:conclusie} meegenomen als aanbeveling voor productie: \emph{onderscheid "geen data" van "alles is goed"}.

\section{Resourceverbruik}
\label{sec:poc-resource}

Op 15 april werd het resourceverbruik van de volledige stack gemeten op een 2~vCPU / 4~GB-VM met tien draaiende containers onder idle-load (vóór de NDI-integratie). Tabel~\ref{tab:resource} toont de belangrijkste cijfers; de twee NDI-containers die op 19 april zijn toegevoegd verhogen de totale belasting met naar schatting enkele procenten CPU en een honderdtal MiB RAM --- ruim binnen de resterende capaciteit van de VM.

\begin{table}[htbp]
  \centering
  \caption[Resourceverbruik van de stack]{Resourceverbruik onder idle-load (2 vCPU, 4 GB RAM, AlmaLinux~9).}
  \label{tab:resource}
  \begin{tabular}{l r r}
    \hline
    \textbf{Resource} & \textbf{Gebruik} & \textbf{Capaciteit} \\
    \hline
    RAM (alle containers samen) & 977 MiB & 3\,892 MiB (25\%) \\
    CPU (10 containers, pre-NDI) & $\sim$130\% van 1~vCPU & 200\% (2 vCPU) \\
    Disk (Loki + Prometheus)    & $<$250 MiB & 19 GB \\
    \hline
  \end{tabular}
\end{table}

De twee grootste verbruikers zijn \texttt{incontrol2-exporter} (scrape-bursts per 15~s) en \texttt{srt-test-stream} (FFmpeg-encoding). In productie vervalt die laatste containerlaag --- de echte SRT-encoder levert de stream --- wat ongeveer 55\% CPU en 240~MiB RAM vrijmaakt. De aanbevolen productie-sizing voor circa tien sites op een scrape-interval van 15~s is dan ook 2~vCPU / 2~GB~RAM / 20~GB~disk. Het systeem schaalt lineair met het aantal targets.

\section{Scope-afbakening en bekende beperkingen}
\label{sec:poc-scope}

Om de verdedigbaarheid van de PoC transparant te houden, worden de belangrijkste scopegrenzen hieronder expliciet benoemd.

\paragraph{Balance 20X als hybride Live3-spoke.} De fysieke Balance~20X werd oorspronkelijk opgenomen als een vijfde volwaardige site met een P2P PepVPN-tunnel naar FH-Bornem. Die tunnel bleef door een combinatie van een verlopen PrimeCare-licentie en een IC2-locked profiel in een authenticatie-cyclus steken en werd op 16 april opgegeven. De Balance~20X is daarna bewust behouden \emph{buiten} het SpeedFusion-profiel en bereikt de observability-stack via een bridged WiFi-adapter (\texttt{192.168.1.1}). Die keuze levert nog steeds twee bewijsdoelen op: \emph{(i)} de stack werkt op fysieke hardware met enterprise-SNMP en lokale API, en \emph{(ii)} het IC2-device-online=0-patroon (terwijl SNMP/API/ICMP gewoon groen blijven) is een sterk argument voor de multi-bron aanpak, want een single-source monitoring had hier het verkeerde beeld gegeven.

\paragraph{NAT-traversal: gedeeltelijk experimenteel bevestigd.} De basisopstelling van de virtuele testomgeving gebruikt directe routering via VyOS zonder NAT tussen de spokes. Om toch een empirische basis te leggen onder de drie connectiviteitsscenarios uit sectie~\ref{sec:fase5}, werd één expliciet asymmetrisch NAT-scenario toegevoegd (scenario~7, sectie~\ref{sec:poc-tests}): FH-Live1 werd via een extra \texttt{source NAT}-regel op VyOS achter een tweede masquerade-laag geplaatst, zodat zijn verkeer richting de Bornem-hub als "afkomstig van de gateway" binnenkomt in plaats van met zijn eigen spoke-IP. Dat volstaat om de kernclaim van de observability-laag te valideren: zolang \texttt{peplink\_tunnel\_up=1} blijft de stack routing-agnostisch, omdat alle exporters op tunneladressen (\texttt{10.1.x.x}) werken. Wat \emph{niet} in deze PoC geëvalueerd wordt, is PepVPN's eigen NAT-T-implementatie onder extreme condities (CGNAT-hole-punching, port-rebind-frequentie, keepalive-tuning): dat is commerciële productfunctionaliteit van Peplink waarvoor publieke documentatie en leverancierstesten bestaan. Een korte tunnelflap door NAT-rebinding wordt operationeel opgevangen door de alert-drempel van één minuut.

\paragraph{Syslog van de Balance 20X locatie-afhankelijk.} Omdat de syslog-collector op de bridged WiFi-adapter van de observability-VM luistert, wijzigt het doel-IP per netwerk. Bij een werkende tunnel zou een vast \texttt{10.1.1.100}-adres volstaan. In productie is dit geen issue omdat de observability-host een vast IP heeft.

\paragraph{SMTP voor alert-notificaties.} De Grafana-alerting is gekoppeld aan een email-contactpoint, maar er is in het PoC geen SMTP-server geconfigureerd. Alerts zijn dus zichtbaar in de Grafana-UI maar worden niet effectief verzonden. Voor productie volstaat het toevoegen van \texttt{GF\_SMTP\_*}-variabelen, of een Teams/Slack-webhook.

\paragraph{NDI geïntegreerd, wel met virtuele bron.} NDI-monitoring is niet langer een toekomstige uitbreiding: de \texttt{ndi-exporter} draait volledig operationeel en exporteert frame-level metrics via de officiële NDI SDK. De bron in het PoC is wél een synthetische Python-sender (SMPTE-kleurbalken) op dezelfde observability-VM, niet een hardware-encoder in een productie-LAN. Dat wijzigt niets aan de code van de exporter --- hij abonneert zich op een bronnaam, ongeacht of die van een camera, een mixer of een testscript komt --- maar levert wel twee nuances op. Ten eerste is mDNS in de PoC enkel link-local getest; voor cross-VLAN-detectie biedt NDI de \emph{Discovery Server} aan, die in Mediaventures een vervolgtraject is. Ten tweede vertoont de synthetische loopback-opstelling een restdrop-rate van 10--15\% die te wijten is aan de timing-precisie van de Python-sender en niet aan de meting zelf; in een productiescenario met een hardware-encoder verdwijnt die baseline.

\section{Synthese}
\label{sec:poc-synthese}

Het proof-of-concept valideert dat een open-source PLG-stack in combinatie met een custom Python-exporter in staat is om de volledige audiovisuele keten van Mediaventures end-to-end te bewaken. De vijf uitgevoerde testscenario's tonen aan dat storingen op de netwerklaag, de device-laag én de applicatielaag binnen 1 tot 5 minuten gedetecteerd en gevisualiseerd worden. De zelf-validatie rondom de \texttt{peplink\_tunnel\_up}-blindspot bevestigt bovendien dat een iteratieve, kritische opbouw van de stack noodzakelijk is: elke metric moet gevalideerd worden tegen de grond-waarheid, niet enkel tegen wat het dashboard toont. Het resultaat is een overdraagbaar prototype dat na deze bachelorproef zonder fundamentele aanpassingen naar de productieomgeving van Mediaventures kan doorgroeien.
